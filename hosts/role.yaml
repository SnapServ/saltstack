hosts:
  managed: False

  hostname: {{ salt['grains.get']('host') }}
  domain: {{ salt['grains.get']('domain') }}
  primary_address4: {{ salt['ss.default_network_address']().v4|yaml_encode }}
  primary_address6: {{ salt['ss.default_network_address']().v6|yaml_encode }}

  <os_family>:
    Debian:
      hosts_path: '/etc/hosts'
      hostname_path: '/etc/hostname'
      hostname_cmd_fmt: 'hostnamectl set-hostname {:s}'
